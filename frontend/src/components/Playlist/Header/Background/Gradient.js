import styled from 'styled-components'

const Gradient = styled.div`
  position: absolute;
  height: 100%;
  width: 100%;
  top: 0;
  left: 0;
  background: linear-gradient(270deg, rgba(0, 0, 0, 0), black 90%);
`

export default Gradient
