import React from 'react'
import { getLoggedUserProfile } from 'api/Me'

function Me() {
  // getLoggedUserProfile().then((res) => {
  //   console.log(res)
  // })

  return <p>Me page; access token:</p>
}

export default Me
